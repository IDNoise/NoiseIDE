- id: get_prop
  desc: State prop getter
  snippet: |
    $prop$(State) ->
        state:prop(State, $prop$).

- id: set_prop
  desc: State prop setter
  snippet: |
    set_$prop$(State, $var$) ->
        state:set_prop(State, $prop$, $var$).